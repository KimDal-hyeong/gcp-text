{
  "name": "now-test",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KimDal-hyeong/now-test.git"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
